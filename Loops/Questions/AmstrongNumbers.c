// Q. Print all Amstrong numbers btw 1-500
// Amstrong numbers -> Sum of cube of all digits in the numebr is equal to the number itself is known as Amstrong numbers.

#include<stdio.h>

int main(){
    int n;
    printf("Enter a number :");
    scanf("%d",&n);
    while (n != 0){
        
    }
    
    return 0;
}
