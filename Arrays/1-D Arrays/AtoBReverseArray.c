// Make an array 'A' and store elements in it and print same elements but in reverse order in array B.

#include<stdio.h>

int main(){
    int n; scanf("%d", &n);
    int a[n];
    int b[n];
    for (int i = 0; i <= 6; i++) {
        scanf("%d", &a[i]);
    }
    for (int i = 0; i <= 6; i++) {
        scanf("%d", &a[i]);
    }
    
    return 0;
}